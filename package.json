{
  "name": "blocky-writer",
  "version": "0.1.0",
  "private": true,
  "description": "Firefox extension for block-based PDF form filling using ReScript + Rust/WASM",
  "scripts": {
    "setup": "deno install",
    "build:res": "deno task build:res",
    "watch:res": "deno task watch:res",
    "build:wasm": "deno task build:wasm",
    "build:js": "deno task build:js",
    "build": "deno task build",
    "dev": "deno task dev",
    "lint": "deno task lint"
  },
  "dependencies": {
    "@rescript/react": "^0.13.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "rescript-webapi": "^0.9.1"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^12.0.2",
    "rescript": "^11.1.4",
    "webpack": "^5.98.0",
    "webpack-cli": "^6.0.1",
    "web-ext": "^8.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
